Python practice, URL price monitor tool
-Current features:
-> Price tracker (amazon only, searching by class)

->Next Goals:
-SQLite local db to store price history
-CLI commands to add more products to config.json
-Expand tracking method